# --- ВЕБ-ФРЕЙМВОРК (API) ---
# Современный стандарт для ML-проектов. Быстрый и поддерживает асинхронность.
fastapi==0.109.0
uvicorn[standard]==0.27.0  # Сервер для запуска FastAPI
pydantic==2.6.0            # Валидация данных (проверка, что прислали именно строку/число)
python-multipart==0.0.9    # Нужно для загрузки файлов (UploadFile) через API

# --- БАЗА ДАННЫХ (PostgreSQL) ---
sqlalchemy==2.0.25         # ORM: работа с БД через Python-классы, а не сырой SQL
psycopg2-binary==2.9.9     # Драйвер для подключения к PostgreSQL
alembic==1.13.1            # Миграции: чтобы менять структуру таблиц без боли

# --- РАБОТА С ФАЙЛАМИ И S3 ---
boto3==1.34.34             # SDK для AWS S3 (или MinIO/Supabase Storage)
requests==2.31.0           # Для скачивания файлов по ссылкам

# --- ОКРУЖЕНИЕ И НАСТРОЙКИ ---
python-dotenv==1.0.1       # Чтобы читать пароли из файла .env (не хардкодить их!)

# --- ОБРАБОТКА ИЗОБРАЖЕНИЙ И ВИДЕО (База для AI) ---
numpy==1.26.3              # Математика, матрицы (фундамент для всего ИИ)
pandas==2.2.0              # Работа с табличными данными
Pillow==10.2.0             # Обработка изображений (ресайз, конвертация форматов)
# Используем headless версию (без GUI), чтобы не было ошибок на сервере:
opencv-python-headless==4.9.0.80  # Мощнейшая библиотека для компьютерного зрения (видео/фото)

# --- ИСКУССТВЕННЫЙ ИНТЕЛЛЕКТ (Опционально) ---
# Эти библиотеки нужны, если нейронка крутится прямо здесь, а не на отдельном сервере.
# scikit-learn==1.4.0      # Классическое машинное обучение
# transformers==4.37.0     # Hugging Face: работа с трансформерами (BERT, ViT и др.)
# librosa==0.10.1          # Обработка аудио (для поиска голосовых дипфейков)